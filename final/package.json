{
  "name": "portfolio-fullstack",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf client/node_modules && npm --prefix client install && npm --prefix client run build && npm --prefix server install",
    "start": "node server/server.js"
  },
  "engines": {
    "node": "22.x"
  }
}
