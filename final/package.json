{
  "name": "portfolio-fullstack",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm --prefix client install && npm --prefix client run build && cp -r client/dist server/public && npm --prefix server install",
    "start": "node server/server.js"
  },
  "engines": {
    "node": "20.x"
  }
}
