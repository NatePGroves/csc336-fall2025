{
  "name": "portfolio-fullstack",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm --prefix client install && cd client && npx vite build && cp -r dist ../server/public && cd ../server && npm install",
    "start": "node server/server.js"
  },
  "engines": {
    "node": "20.x"
  }
}